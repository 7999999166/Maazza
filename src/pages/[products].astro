---
// use data & info combo .
// Use map(); Method With Array to do the Work .
// Two things are important to Define in dynamic routes . 
// 1. params we define are used as name of the routes .
// 2. props are use to fill up the page .
export function getStaticPaths() {
  return [
    {params: {dog: 'clifford'}},
    {params: {dog: 'rover'}},
    {params: {dog: 'spot'}},
  ];
}

const { dog } = Astro.params;



---

<div>Good dog, {dog}!</div>
